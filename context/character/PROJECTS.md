# character 관련 프로젝트

| 레포 | 역할 | 담당 |
|------|------|------|
| `mohashim` (현재 모노레포) | `src/components/Potato.tsx` 5단계 SVG + `src/lib/phrases.ts` 8버킷 멘트 | bonseung |

## 시안 핸드오프

- `Mohashim Design.html` 동봉 `potato.jsx` — 5단계 표정 + 새싹(sprout) + tray 아이콘 SVG 모두 작성됨. 코드 포팅 시 그대로 활용
- `popup.jsx` 내 `POTATO_PHRASES` 상수 — 8버킷 멘트 사전
